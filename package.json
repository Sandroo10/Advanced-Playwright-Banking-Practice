{
  "name": "apex-bank-playwright-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:auth": "playwright test tests/auth",
    "report": "playwright show-report",
    "codegen": "playwright codegen https://playwrightpad.com/sandbox/banking"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3"
  }
}
